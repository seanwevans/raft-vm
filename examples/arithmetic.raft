# compute ((5 - 3) * 2) / 4 and store it in slot 0
5 3 - 2 * 4 / StoreVar 0

# load the stored result twice, swap, and drop the duplicate
LoadVar 0
Dup
Swap
Pop

# finish with a modulus and negation example
7 % Neg
